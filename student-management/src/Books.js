import React, { Component } from 'react'
import ApolloClient from 'apolllo-boost'
import { ApolloProvider } from 'react-apollo'

export default class Books extends Component {
    render() {
        return (
            <ApolloProvider>
            <div>
                
            </div>
            </ApolloProvider>
        )
    }
}
